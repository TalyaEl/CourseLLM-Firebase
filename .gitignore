# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# ============================================================================
# 1. Dependency & Build Output (Node.js / Next.js)
# ============================================================================

# Dependencies and lock files
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
package-lock.json

# Build and production output
/.next/
/out/
/build
/dist
*.tsbuildinfo

# Debug, Logs & Cache
/coverage
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log             # Added from the new version (catches all .log files)

# ============================================================================
# 2. Environment Variables & Secrets (NEVER COMMIT THESE!)
# ============================================================================

# Next.js environment variables (combined and simplified)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local         # Catches all .env.local variations
.env* # Added from the new version (catches all .env files)

# Python/DSPy service environment variables
dspy_service/.env
dspy_service/.env.local

# Allow example env files for documentation (Negative rules)
!.env.example
!dspy_service/.env.example
!.env.local.example # Added from the new version

# ============================================================================
# 3. Python / DSPy Service
# ============================================================================

# Python virtual environments
.venv/
venv/
dspy_service/.venv/
dspy_service/venv/
env/
ENV/

# Python cache and compiled files
__pycache__/
*.py[cod]
*$py.class
*.so

# Python egg info
*.egg-info/
dist/
build/
.pytest_cache/

# ============================================================================
# 4. Specific Tooling & Services
# ============================================================================

# Vercel
.vercel

# Firebase
firebase-debug.log
firestore-debug.log
firebase-debug.*.log

# TypeScript
next-env.d.ts

# Genkit
.genkit/*

# Dataconnect (Added from the new version)
dataconnect/.dataconnect
src/dataconnect-generated/
src/dataconnect-admin-generated/

# ============================================================================
# 5. Local Development Data & Misc
# ============================================================================

# JSON storage for demo mode (contains generated IST events)
src/mocks/ist/events.json

# Misc files
*.pem

# ============================================================================
# 6. Operating System Files (Deduplicated)
# ============================================================================

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
Desktop.ini

# Linux
*~